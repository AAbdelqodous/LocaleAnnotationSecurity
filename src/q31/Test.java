package q31;

import q30.TrafficLight;
import q30.TrafficSignal;

import java.util.stream.Stream;

public class Test {
    public static void main(String[] args) {
        var chess = new Chess();

//        var tf = chess.getClass().getAnnotation(ChessRules.class);
//        Stream.of( tf.value()).forEachOrdered(System.out::println);
    }
}
